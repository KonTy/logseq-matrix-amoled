module.exports = {
  branches: ["main"],
  plugins: [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/github",
    {
      path: "@semantic-release/npm",
      npmPublish: false
    }
  ],
  npmrc: false // Add this line to disable generating .npmrc file
};
          
